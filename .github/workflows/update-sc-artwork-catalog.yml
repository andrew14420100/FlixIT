name: Update SC artwork catalog

on:
  workflow_dispatch:
  schedule:
    - cron: "17 4 * * *"
  push:
    branches:
      - main
    paths:
      - scripts/build_sc_artwork_catalog.py
      - scripts/build_sc_artwork_catalog_v4.py
      - .github/workflows/update-sc-artwork-catalog.yml

permissions:
  contents: write

concurrency:
  group: sc-artwork-catalog
  cancel-in-progress: true

jobs:
  build-catalog:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      SC_ARTWORK_REQUEST_DELAY: "0.75"
      SC_ARTWORK_TRANSIENT_RETRIES: "8"
      SC_ARTWORK_API_MAX_PAGES: "600"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build complete StreamingCommunity artwork index
        run: python scripts/build_sc_artwork_catalog_v4.py --minimum 17000 --output backend/data/sc_artwork_catalog.json
      - name: Validate catalog coverage
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          path = Path("backend/data/sc_artwork_catalog.json")
          data = json.loads(path.read_text(encoding="utf-8"))
          count = int(data.get("count") or 0)
          print(f"SC catalog entries: {count}")
          if count < 17000:
              raise SystemExit(f"Catalog incomplete: expected at least 17000 artwork titles, got {count}")
          PY
      - name: Publish same catalog as a static frontend asset
        run: cp backend/data/sc_artwork_catalog.json frontend/public/sc-artwork-catalog.json
      - name: Commit updated catalog
        run: |
          if git diff --quiet -- backend/data/sc_artwork_catalog.json frontend/public/sc-artwork-catalog.json; then
            echo "Catalog already up to date"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/data/sc_artwork_catalog.json frontend/public/sc-artwork-catalog.json
          git commit -m "chore: refresh SC artwork catalog"
          git push
